name: Run tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Run tests
        env:
          MIN_COVERAGE: 80
        run: npm test -- --coverage --coverageThreshold=${MIN_COVERAGE}
